const CONFIG = {
    // Public LibreTranslate instance (may require API key or have rate limits)
    // You can host your own: https://github.com/LibreTranslate/LibreTranslate
    API_URL: 'https://libretranslate.de/translate',
    API_KEY: '', // Leave empty if not required by the instance
    DEFAULT_LANG: 'zh-TW'
};
